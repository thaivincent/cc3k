module decorator;
import <iostream>;
import <string>;
import statuseffect;

Decorator::~Decorator() {delete next;}

int  Decorator::getAtk(PC &pc) {
    //need accessor method for atk
}

int  Decorator::getDef(PC &pc) {
    //need accessor method for def
}
